<%= button_to("Nova finança",category_new_path,{class: 'btn btn-success', method: :get}) %>
<br>
Saldo Atual: <% if !@movements.empty? %>
                <%= @movements.first.balance %> 
             <% end %>

<table class="table">
  <thead>
    <tr>
      <th>Categoria</th> 
      <th>Valor</th> 
    </tr>
  </thead>
  <tbody>
      <% @movements.each do |m| %>
        <tr>
          <td><%= m.category.name %><br><%= m.date_launched %></td>
          <% if m.category.type_category == "R" %>
            <td>+<%= m.balance %></td>
          <% else %>
            <td><%= m.balance %></td>
          <% end %>
        </tr>
      <% end %>
  </tbody>
</table>
